extends ./layout.jade

block title
  = title

block content
  .container-fluid
    header#blogheader
      div.logo
        h1
          a(href='/') Flauschiversum
        p.description Mehr als nur eine Masche
          small immer Sonntags

      div.category-images
        if !category || category == 'basteln'
          a(href='/basteln/')
            h2 Basteln
            img.category(src='/images/Basteln.jpg')
            p Mit Klebe und Schere bestückt,<br/> wird mit Liebe geschmückt.
        if !category || category == 'brandings'
          a(href='/brandings/')
            h2 Brandings
            img.category(src='/images/Brandings.jpg')
            p Die Haut zu dick? Das ist egal,<br/> bebrannt wird auch das Holzregal.
        if !category || category == 'filzen'
          a(href='/filzen/')
            h2 Filzen
            img.category(src='/images/Filzen.jpg')
            p Wasser, Seife, Wolle,<br/> reibe nicht zu dolle.
        if !category || category == 'malen'
          a(href='/malen/')
            h2 Malen
            img.category(src='/images/Malen.jpg')
            p Pinselstrich und Mine, geben mir die Muße,<br/> dem ersten Zug, ein Neuer folgt zum Gruße.
        if !category || category == 'naehen'
          a(href='/naehen/')
            h2 Nähen
            img.category(src='/images/Naehen.jpg')
            p Mit Nadel und Faden in den Krieg,<br/> erreichen kannst du nur den Sieg.
        if !category || category == 'wolle'
          a(href='/wolle/')
            h2 Wolle
            img.category(src='/images/Wolle.jpg')
            p Spinne, häkel, webe, sticke,<br/> knüpfe dir nur keine Stricke.

    main
      each post in posts
        .center
          article
            header
              p.date
                span.date= moment.utc(post.date).format('DD. MMMM YYYY')
              a(href=post.url)
                h2= post.title
                if post.image
                  img.post-image(src=post.image)
                  
            p.excerpt= post.excerpt
            p.more
              a(href=post.url) weiterlesen
      include ./lib/pagination.jade




